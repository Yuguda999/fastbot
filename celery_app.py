# from celery import Celery

# celery_app = Celery("fastbot", broker="redis://localhost:6379/0")

# celery_app.autodiscover_tasks(["tasks"])  # Ensure tasks are registered

# import tasks  # Importing explicitly to avoid unregistered task issues
